name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    name: Build apps
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install orc
        # orc-cli is a private repo; ORC_CLI_TOKEN is a PAT with read access to admte/orc-cli.
        run: ./scripts/install.sh
        env:
          GITHUB_TOKEN: ${{ secrets.ORC_CLI_TOKEN }}
      - name: Build every app (validates artifact.yaml + config schema)
        run: |
          for dir in apps/*/; do
            app=$(basename "$dir")
            echo "::group::build $app"
            ./orc build "$dir" --output "/tmp/oci-$app"
            echo "::endgroup::"
          done
