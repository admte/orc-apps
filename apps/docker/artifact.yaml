artifactType: application/vnd.orc8r.app.v1
annotations:
  org.opencontainers.image.title: docker
  # org.opencontainers.image.version: 1.0.0    # optional; omitted -> tag "default"
  org.opencontainers.image.description: Install the Docker engine
config:
  install:
    command: sh install-docker.sh
files:
  - install-docker.sh
platforms:
  - os: linux
    arch: amd64
  - os: linux
    arch: arm64
